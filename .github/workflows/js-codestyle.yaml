name: Code style JavaScript

on:
   push:
      branches-ignore:
         - '**-dev'
   pull_request:

jobs:

  semistandard:
    runs-on: ubuntu-latest
    name: SemiStandard

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - uses: actions/setup-node@v6

      - run: npm install semistandard snazzy

      - name: Run semistandard
        run: npm run eslint